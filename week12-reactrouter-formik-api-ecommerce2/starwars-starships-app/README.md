# Star Wars Starships App

> Project scaffold only.  
> This repository contains the initial folder structure and empty files for a React application that will list, search, and display Star Wars starships using the SWAPI.tech API.  
> All CSS files are managed separately in the `/styles` folder.
